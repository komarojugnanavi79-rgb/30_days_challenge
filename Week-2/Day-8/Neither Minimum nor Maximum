class Solution:
    def findNonMinOrMax(self, nums):
        if len(nums) <= 2:
            return -1

        mini = min(nums)
        maxi = max(nums)

        for num in nums:
            if num != mini and num != maxi:
                return num

        return -1
